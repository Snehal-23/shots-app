import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  Dimensions,
  FlatList,
  View,
} from 'react-native';

const Camera = () => {
  return (
    <View>
      <Text>Camera</Text>
    </View>
  );
};

export default Camera;
